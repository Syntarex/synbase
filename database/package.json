{
    "name": "@synbase/database",
    "private": true,
    "main": "src/index.ts",
    "scripts": {
        "build": "prisma generate",
        "dev": "docker run --rm -p 5432:5432 --env-file ../.env --name synbasedb postgres",
        "start": "prisma migrate deploy",
        "sync": "prisma migrate dev --name init"
    },
    "devDependencies": {
        "prisma": "^5.9.1",
        "ts-node": "^10.9.2"
    },
    "dependencies": {
        "@prisma/client": "^5.10.0"
    }
}
