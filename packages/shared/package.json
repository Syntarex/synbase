{
    "name": "@synbase/shared",
    "version": "0.0.1",
    "private": true,
    "main": "lib/index.js",
    "types": "lib/index.d.ts",
    "files": [
        "lib"
    ],
    "dependencies": {
        "axios": "^0.26.1",
        "lodash": "^4.17.21"
    },
    "devDependencies": {
        "@types/lodash": "^4.14.180"
    },
    "scripts": {
        "prebuild": "yarn clean",
        "prepack": "yarn build",
        "build": "tsc --build",
        "clean": "rimraf lib && rimraf tsconfig.tsbuildinfo"
    }
}
