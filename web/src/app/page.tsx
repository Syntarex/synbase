import { Typography } from "@mui/material";
import { NextPage } from "next";

const Home: NextPage = () => {
    return (
        <main>
            <Typography>Nothing to see</Typography>
        </main>
    );
};

export default Home;
